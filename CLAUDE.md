# eFootball Team AI Claude Code Rules

## 最重要ルール

Claude Codeがアクセスしてよい範囲は、
現在開いているeFootball-Team-AIフォルダと、
その配下のファイルおよびフォルダだけです。

現在のワークスペース外には、
仕事関係の重要なファイルが存在します。

理由にかかわらず、
ワークスペース外のファイルを読み取り、検索、作成、編集、
削除、移動、コピー、実行しないでください。

## 許可された作業範囲

Claude Codeが操作してよい場所:

- 現在のeFootball-Team-AIフォルダ
- eFootball-Team-AI配下にあるファイル
- eFootball-Team-AI配下に新しく作成するファイル
- eFootball-Team-AI配下に新しく作成するフォルダ

## 禁止された作業範囲

Claude Codeは、以下へアクセスしないでください。

- eFootball-Team-AIの親フォルダ
- eFootball-Team-AIの兄弟フォルダ
- デスクトップ上の他のファイルやフォルダ
- Documents
- ドキュメント
- Downloads
- ダウンロード
- OneDrive内の他の場所
- 仕事用フォルダ
- 他のプロジェクト
- Cドライブ直下
- ユーザーフォルダ全体
- 他のドライブ
- USBストレージ
- 外付けストレージ
- ネットワークドライブ
- 共有フォルダ
- クラウド同期フォルダ
- 現在のワークスペース外にある全ての場所

## 禁止操作

以下の操作は禁止します。

- cd ..
- ../を使用したアクセス
- 親フォルダへの移動
- 絶対パスを使ったワークスペース外へのアクセス
- ワークスペース外のファイル一覧取得
- ワークスペース外のファイル検索
- ワークスペース外のファイル読取り
- ワークスペース外でのファイル作成
- ワークスペース外のファイル編集
- ワークスペース外のファイル削除
- ワークスペース外へのファイル移動
- ワークスペース外へのファイルコピー
- デスクトップ全体の検索
- OneDrive全体の検索
- Documents全体の検索
- Downloads全体の検索
- ユーザーフォルダ全体の検索
- ドライブ全体の検索
- grep、find、Get-ChildItemなどを使用した広範囲の検索
- シンボリックリンクやジャンクションを経由した外部アクセス
- 管理者権限の要求
- 管理者権限でのコマンド実行
- ワークスペース外へのファイル出力
- 仕事関係のファイルへのアクセス

## コマンド実行前の義務

コマンドを実行する前に、
必ず以下を日本語で報告してください。

1. 現在の作業ディレクトリ
2. 実行する正確なコマンド
3. コマンドの目的
4. 読み取るファイル
5. 作成するファイル
6. 編集するファイル
7. 削除するファイル
8. 外部アクセス先
9. ワークスペース外へアクセスしないこと
10. 元に戻す方法
11. 実行結果の確認方法

私が明示的に承認するまで、
コマンドを実行しないでください。

## ファイル操作前の義務

ファイルの作成、編集、削除、移動、コピーを行う前に、
対象となる全ファイルを相対パスで報告してください。

正しい相対パスの例:

- ./src/app/page.tsx
- ./src/components/sidebar.tsx
- ./docs/efootball-team-ai-design.md
- ./package.json

対象がeFootball-Team-AI配下であることを確認できない場合は、
操作を行わないでください。

私が明示的に承認するまで、
ファイル操作を開始しないでください。

## 削除に関する制限

私が削除対象を明示的に承認するまで、
ファイルやフォルダを削除しないでください。

以下も削除または破壊的操作として扱ってください。

- ファイルの完全な置換
- フォルダの初期化
- データベースの初期化
- データベースの全件削除
- package.jsonの再作成
- lockファイルの削除
- Git履歴の破棄
- Gitの強制リセット
- 既存コードの全面的な書き換え
- 既存ファイルの上書き

## 外部アクセス前の義務

外部サイトまたは外部APIへアクセスする前に、
必ず以下を日本語で報告してください。

1. アクセス先URL
2. HTTPメソッド
3. 送信するデータ
4. リクエスト回数
5. 取得する内容
6. 保存するファイル
7. タイムアウト
8. 停止方法
9. ワークスペース外へ保存しないこと

私が明示的に承認するまで、
外部アクセスを行わないでください。

## パッケージインストール前の義務

npm、npx、pnpm、yarn、pipなどを使用する前に、
必ず以下を報告してください。

1. インストールするパッケージ名
2. バージョン
3. 用途
4. 変更されるファイル
5. 作成されるフォルダ
6. インストールコマンド
7. アンインストール方法
8. ワークスペース外へ変更を加えないこと

グローバルインストールは禁止します。

許可する場合も、
現在のeFootball-Team-AIワークスペース内への
ローカルインストールだけを使用してください。

## 機密情報を発見した場合

以下に該当する可能性があるファイルを発見した場合は、
内容を開かないでください。

- 仕事関係のファイル
- 個人情報
- パスワード
- APIキー
- Cookie
- アクセストークン
- 認証情報
- 秘密鍵
- 決済情報
- 無関係なログ
- 第三者アカウント情報

発見した場合は次を行ってください。

1. 内容を開かない
2. 内容を表示しない
3. 内容を要約しない
4. 相対ファイル名だけを報告する
5. 直ちに作業を停止する
6. 私の指示を待つ

## 自動承認の禁止

自動承認、権限バイパス、
確認なしのコマンド実行、
確認なしのファイル編集を前提にしないでください。

各操作について、
必ず私の明示的な承認を待ってください。

## 作業開始時の確認

新しい作業を開始する前に、
次を確認してください。

- 現在の作業対象はeFootball-Team-AI内だけである
- ワークスペース外を使用しない
- 親フォルダへ移動しない
- 絶対パスで外部へアクセスしない
- 仕事関係のファイルを操作しない
- 操作前に承認を求める

## ビルド・キャッシュ・プロセスの安全運用

詳細は ./docs/safe-build-and-cache-policy.md を参照してください。要点:

- このフォルダはOneDrive同期対象の可能性が高いです。
- .nextを通常作業で削除しないでください（自動削除禁止）。
- キャッシュ削除（.next、.next/cache、npm cache clean など）はユーザーの明示承認制です。
- 再帰削除・ワイルドカード削除を実行しないでください。
- 10項目以上を削除する可能性がある操作は実行せず停止して報告してください。
- OneDriveの大量削除警告が表示されたら即時停止してください。「すべて削除」を選ばず「保持する」を選択し、削除確認を無効化しないでください。
- ビルド失敗時に.next削除で回避しないでください。削除が必要と判断したら実行せず停止して報告してください。
- 既存の.nextのままnpm run buildが成功した場合は正式な成功結果として扱ってください。
- devサーバーは起動した親PIDを ./data/server.pid に記録してください。
- ポート3000のリスナーPIDを別途確認し、親PIDまたはその子孫であることを確認してください。
- Nodeプロセスを名前単位で一括停止しないでください（Get-Process node | Stop-Process 等）。
- 停止は確認済みの単一の数値PIDで行い、完了報告には実際の数値PIDとコマンドを書いてください（プレースホルダー禁止）。実際に停止していない場合は「Node停止なし」と報告してください。

これらは既存の安全規則を弱めるものではなく、追加の制約です。

## 不明な場合

操作がeFootball-Team-AI内だけで完結するか判断できない場合は、
その操作を実行しないでください。

推測で進めず、
問題点を日本語で説明して私の指示を待ってください。

この安全ルールは、
このプロジェクトに関する他の指示よりも優先してください。

## 参照文書（Claude Code が最初に読むもの）

この `CLAUDE.md` は最優先の絶対安全規則です。以下の文書は、これまで毎回のプロンプトへ
再掲していた共通ルールを整理した**単一の真実源**であり、`CLAUDE.md` を弱めるものではありません。

新しいマイルストーンを開始するときは、次を読んでから作業してください。

- `./docs/development-safety-policy.md` — 開発安全規則（ワークスペース・データ保護・保存互換性・
  計算保護・更新直前の整合性検証・storage イベント・Node 管理・共通の重大停止条件）
- `./docs/quality-gates.md` — 品質ゲート（実装中の確認・最終品質ゲート 20 手順・`npm run verify` の
  重複排除・全13ブラックボックスと dev 固有差の切り分け・SQLite `integrity_check`・完了判定・
  Markdown だけの変更時のベースライン継承）
- `./docs/project-baseline.md` — 現在の完全成功ベースライン（テスト件数・SQLite 件数・保護スキーマ・
  localStorage キー・ポジション別 OVR 状態）
- `./docs/milestone-workflow.md` — マイルストーン実行手順・使用量不足時の安全な中断/再開・
  新セッション引き継ぎ・標準/短縮プロンプト構造・完了報告の二層構造
- `./docs/safe-build-and-cache-policy.md` — ビルド・キャッシュ・`.next`・OneDrive・Node プロセス

安全規則の優先順位:
1. ユーザーがそのプロンプトで明示した安全条件
2. この `CLAUDE.md`
3. `./docs/development-safety-policy.md`
4. そのマイルストーン固有の重大停止条件
5. `./docs/quality-gates.md`
6. マイルストーン要件
7. ドキュメント・報告形式
8. 使用量最適化

使用量削減を理由に、上位（1〜6）の安全規則・品質ゲート・機能要件を弱めないでください。
迷った場合は安全性とデータ保護を優先し、曖昧な場合は推測せず停止してください。

正本ワークスペースは `C:\Development\eFootball-Team-AI`（OneDrive 同期対象外）です。
旧 `.../OneDrive/デスクトップ/eFootball-Team-AI` はバックアップであり、開く・読み取る・編集する等を
一切行わないでください。